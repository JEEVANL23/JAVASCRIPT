// assignment operator
var a = 10;
console.log(a)

// arithmetic operators
var b = 20;
sum = a+b;  // addition
console.log(sum);

dif = b-a;      // subtract
console.log(dif)

mull = a*b;     // multiplication
console.log(mull);

divv = b/a;     // division
console.log(divv);

modd = b%a;     // modulus 
console.log(modd);

a++;  // increment operator
console.log(a);

b--; // decrement operator
console.log(b);


// comparision operator notes

// == is equal to
// === identical and same type as well
// != not equal to
// !== not identical
// > greater than
// < less than
// >= greater than or equal to
// <= less than or equal to


// string operators

// only strings can be concatenated or it may be added thats it


// logical operators obtain us the boolean values

/// && and
// || or
// ! not

var c = 5;
var d = 10;

e = (c ==d) || (c>d);
console.log(e)

f = (c<d) && (d>c);
console.log(f);

